/**
 * This package is a temporary location for things we want to extract to other projects. These are
 * either fixes or augmentation that belong to these external projects like summerb or swingpm.
 *
 * <p>We don't want to due this on ad-hoc basis because release cycle of summerb and pgptool are
 * slow.
 *
 * @author sergeyk
 */
package EXPORT.org.summerb.validation;
